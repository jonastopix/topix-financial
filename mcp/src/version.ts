// Single source of truth for the server identity. package.json mirrors this
// version; ping returns it. Keep the two in sync on every release.
export const SERVER_NAME = "boardroom-mcp";
export const SERVER_VERSION = "0.1.0";
