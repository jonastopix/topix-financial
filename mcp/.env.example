# Boardroom MCP — local environment. NEVER commit real values.
#
# The service-role key bypasses RLS (RECON.md §2). It is NOT stored in the repo
# and is NOT the publishable key. Obtain it from the Supabase/Lovable dashboard
# and keep it only in your local, untracked mcp/.env.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
