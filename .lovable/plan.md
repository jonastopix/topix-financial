

## Favicon Update

Copy the uploaded icon to the public directory and update `index.html` to use it as the browser favicon.

**Changes:**
1. Copy `user-uploads://Favocon_Topix.png` → `public/favicon.png`
2. Update `index.html` to add a `<link rel="icon">` pointing to `/favicon.png`

