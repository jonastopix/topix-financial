project_id = "loiavmastgeieqyiwyyr"

[functions]
  # UDKAST 18/9 — e-underskriften. verify_jwt = FALSE, bevidst: kalderen er
  # /aftale — en person UDEN session og uden konto. Beskyttelsen er
  # aftaletokenet i body, verificeret af verifyAftaletoken
  # (_shared/aftaletokenAuth.ts) FØR enhver service-role-handling — samme
  # klasse som opret-indgangs-checkout (verifyBetalingstoken).
  [functions.aftale-underskrift]
    verify_jwt = false
  # Samme begrundelse som get-community-billed-url nedenfor: ny Bucket
  # A-funktion, gatewayen bærer signaturtjekket (PR #267-mønstret).
  # Advisor-gate via has_role FØR service-role-konstruktion.
  [functions.agent-forslag-afgoer]
    verify_jwt = true
  [functions.ai-financial-feedback]
    verify_jwt = false
  [functions.attach-user-to-company]
    verify_jwt = false
  [functions.auth-email-hook]
    verify_jwt = false
  [functions.extract-financial-data]
    verify_jwt = false
  [functions.generate-budget-from-accounts]
    verify_jwt = false
  [functions.generate-budget-scenarios]
    verify_jwt = false
  [functions.handout-ai-feedback]
    verify_jwt = false
  [functions.import-budget-excel]
    verify_jwt = false
  [functions.manage-advisor]
    verify_jwt = false
  [functions.monday-webhook]
    verify_jwt = false
  [functions.process-pending-invitation]
    verify_jwt = false
  [functions.run-company-agent]
    verify_jwt = false
  [functions.send-invitation-email]
    verify_jwt = false
  [functions.send-slack-chat-notification]
    verify_jwt = false
  [functions.send-slack-report-notification]
    verify_jwt = false
  [functions.send-report-reminder]
    verify_jwt = true
  [functions.nudge-report-no-reflection]
    verify_jwt = true
  [functions.send-template-email]
    verify_jwt = false
  [functions.send-slack-handout-notification]
    verify_jwt = false
  [functions.send-slack-feedback-notification]
    verify_jwt = false
  [functions.validate-facts-parity]
    verify_jwt = false
  [functions.auto-create-baseline-budget]
    verify_jwt = false
  [functions.generate-financial-commentary]
    verify_jwt = false
  [functions.send-notification-email]
    verify_jwt = true
  [functions.create-stripe-checkout]
    verify_jwt = false
  [functions.stripe-webhook]
    verify_jwt = false
  [functions.create-free-intro-booking]
    verify_jwt = false
  [functions.create-legat-enrollment]
    verify_jwt = false
  [functions.upgrade-legat-to-member]
    verify_jwt = false
  [functions.generate-ai-forecast]
    verify_jwt = false
  [functions.import-application]
    verify_jwt = false
  [functions.admin-cleanup-test-data]
    verify_jwt = false
  [functions.get-chat-attachment-url]
    verify_jwt = false
  # verify_jwt = true: funktionen læser JWT-claims via authenticateUser, og
  # gatewayen bærer signaturtjekket — samme invariant som CI-værnet
  # scripts/check-verify-jwt-invariant.ts håndhæver. Bevist 10. august
  # (PR #267): med true afviser gatewayen forfalskede signaturer før koden
  # kører; uden blok validerer den intet. get-chat-attachment-url står på
  # false fordi den er ældre end det fund — den skal migreres i et separat
  # spor og er IKKE forbilledet her.
  [functions.get-community-billed-url]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.get-community-fil-url]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.notify-community-svar]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.notify-community-naevnelse]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.notify-community-opslag]
    verify_jwt = true
  [functions.calendly-webhook]
    verify_jwt = false
  # UDKAST 19/9 — eWebinar-webhook: Bucket C, afsenderen er eWebinar (ingen
  # JWT). Beviset er HMAC-signaturen (X-EWebinar-Signature) over den RÅ body,
  # verificeret af verifyEwebinarSignature (_shared/ewebinarSignatur.ts) FØR
  # parsing og FØR service role — samme klasse som calendly-webhook ovenfor.
  [functions.ewebinar-webhook]
    verify_jwt = false
  # Lag 3: Klaviyo-motoren. Bucket A m. verify_jwt = true (PR #267-moenstret):
  # authenticateUser + advisor-gate FOER service role. AEndrer kundekommunikation,
  # saa der skal staa et menneske bag - ogsaa naar lag 4 er en agent.
  [functions.klaviyo-motor]
    verify_jwt = true
  # UDKAST 19/9 — eWebinar-engangsimport: Bucket B (authenticateServiceRole),
  # og verify_jwt = true er PÅKRÆVET for det mønster — gatewayen bærer
  # signaturtjekket, funktionen læser role-claimet (check:verify-jwt låser
  # det). Kaldes fra SQL editoren med service-role-nøglen, som
  # berig-virksomheder. Tørkørsel som standard.
  [functions.ewebinar-import]
    verify_jwt = true
  [functions.bunny-content-admin]
    verify_jwt = false
  [functions.get-video-embed]
    verify_jwt = false
  [functions.generate-weekly-focus]
    verify_jwt = true
  [functions.event-reminders]
    verify_jwt = true
  # Bucket B (10/9): authenticateServiceRole bag gatewayen, kaldt af pg_cron via
  # kald_edge med vault-nøglen — samme værdi som event-reminders.
  [functions.report-review-cron]
    verify_jwt = true
  # UDKAST 18/9 — flyt-event: Bucket A (authenticateUser + advisor-gate),
  # kaldes fra editorens «Gem» når dato/tid ændres på et publiceret event —
  # samme form som publish-event og cancel-event.
  [functions.flyt-event]
    verify_jwt = true
  # Bucket A (10/9): authenticateUser + advisor-gate, kaldes fra editorens
  # «Publicér» med brugerens JWT — samme form som saet-indgangs-prisniveau.
  [functions.publish-event]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor: gatewayen bærer
  # signaturtjekket, authenticateServiceRole læser role-claimet (Bucket B).
  # Samme værdi som event-reminders, hvis pg_cron-job med vault-nøglen
  # beviseligt passerer gaten i prod.
  [functions.intro-reminder-cron]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.opgave-accepter]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.opgave-udskyd]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.opgave-luk]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.foreslaa-opgave]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor. Fase 2 («Én plan»): rådgiveren sætter mål.
  [functions.maal-skriv]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor. «Én plan» 17/9: medlemmet tilføjer selv skridt under sine aktive mål.
  [functions.skridt-tilfoej]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.hent-fornyelsestilbud]
    verify_jwt = true
  # Samme begrundelse som get-community-billed-url ovenfor.
  [functions.opret-fornyelse-checkout]
    verify_jwt = true
  # verify_jwt = FALSE, bevidst: kalderen er /betal — en besøgende UDEN
  # session og dermed uden JWT (de har ikke en konto endnu). Med true ville
  # gatewayen afvise kaldet før koden kører. Beskyttelsen er betalingstokenet
  # i body, verificeret af verifyBetalingstoken (_shared/betalingstokenAuth.ts)
  # mod hent_betalingsdata_til_checkout, som kun svarer når betaling er
  # tilladt — samme klasse som webhook-signaturerne (Bucket C).
  [functions.opret-indgangs-checkout]
    verify_jwt = false
  # UDKAST 18/9 — Bucket A: authenticateUser + advisor-gate, kaldes fra
  # virksomhedssiden med brugerens JWT — samme form som saet-indgangs-prisniveau.
  [functions.send-til-underskrift]
    verify_jwt = true
  # Bucket B: authenticateServiceRole læser role-claimet, gatewayen bærer
  # signaturtjekket — samme begrundelse som intro-reminder-cron. Kun til
  # manuelle kald med service-role-JWT'en; Monday-grenen og prissætningen
  # kalder logikken i samme proces (_shared/indgangsBetalingsmail.ts).
  [functions.send-indgangs-betalingsmail]
    verify_jwt = true
  # Samme begrundelse som intro-reminder-cron: pg_cron-job med vault-nøglen
  # passerer gaten i prod. Tørkørsel som standard.
  [functions.indgangs-paamindelser-cron]
    verify_jwt = true
  # Onboardingens rytme (9/9): samme form som indgangs-paamindelser-cron —
  # Bucket B, authenticateServiceRole, pg_cron-job med vault-nøglen passerer
  # gaten. Tørkørsel som standard. Mail A (dag 0–1) og C (dag 14–20).
  [functions.onboarding-rytme]
    verify_jwt = true
  # legat-reminder-cron (10/9): var kun Deno.cron og har aldrig kørt. Nu
  # Bucket B — authenticateServiceRole, pg_cron via kald_edge passerer gaten.
  # Tørkørsel som standard.
  [functions.legat-reminder-cron]
    verify_jwt = true
  # Samme begrundelse som indgangs-paamindelser-cron: Bucket B,
  # authenticateServiceRole, pg_cron-job med vault-nøglen passerer gaten.
  # Tørkørsel som standard; ren rapport indtil afsendelsen bygges (7/9).
  [functions.fornyelsesvarsel-cron]
    verify_jwt = true
  # Slettefunktionen (Alina-sagen, 8/9): Bucket B, authenticateServiceRole,
  # tørkørsel som standard; pg_cron planlægges FØRST når en tørkørsel er
  # læst. Gaten er på rækken (motoren i _shared/sletning.ts), ikke kun auth.
  [functions.slet-medlemsdata-cron]
    verify_jwt = true
  # Bucket B, samme form som cronen: engangs-berigelse kaldt fra SQL
  # editoren med vault-nøglen. Tørkørsel som standard.
  [functions.berig-virksomheder]
    verify_jwt = true
  # Bucket A (§19 udløser 2): authenticateUser + advisor-gate, gatewayen
  # bærer signaturtjekket — samme begrundelse som get-community-billed-url.
  # Kaldes fra rådgiverfladen med brugerens JWT.
  [functions.saet-indgangs-prisniveau]
    verify_jwt = true
  # Bucket A (10/9): authenticateUser + advisor-gate. Genkører en strandet
  # CSV/XLSX-rapport fra storage på det eksisterende reportId; tørkørsel
  # som standard. Kaldes fra rådgiverfladen med brugerens JWT.
  [functions.genkoer-rapport]
    verify_jwt = true
  [functions.preview-transactional-email]
    verify_jwt = false
  # Ansøgningsformularen (udkast 18/9): verify_jwt = FALSE, bevidst — samme
  # begrundelse som opret-indgangs-checkout: kalderen er en besøgende UDEN
  # session (ingen konto, intet login). Beskyttelsen er ansøgningstokenet i
  # body, verificeret af verifyAnsoegningstoken (_shared/ansoegningToken.ts)
  # mod hent_ansoegning_til_gem (service_role-only), som kun svarer for en
  # åben ansøgning — samme klasse som webhook-signaturerne (Bucket C).
  # Oprettelsen uden token er begrænset (honningfelt + timelofter).
  [functions.ansoegning-gem]
    verify_jwt = false
  # Samme begrundelse som ansoegning-gem: tokenet FØR opslaget, så kun én
  # med en åben ansøgning kan bruge CVR-kvoten.
  [functions.ansoegning-cvr]
    verify_jwt = false
  # UDKAST 18/9 — ventelisten: Bucket A (authenticateUser + has_role advisor
  # FØR service role), kaldes fra rådgiverfladen — samme form som
  # ansoegning-handling. Handlinger: saet, fjern, tilbyd.
  [functions.venteliste-handling]
    verify_jwt = true
  # ANSØGNINGSMOTOREN (udkast 18/9). Bucket A: rådgiverens handlinger
  # (tal_med_dem, afvis, tilbud, afslag, underskrevet, luk, genaabn, afholdt)
  # bag authenticateUser + advisor-gate — samme form som saet-indgangs-prisniveau.
  [functions.ansoegning-handling]
    verify_jwt = true
  # Ansøgerens link EFTER indsendelse: verify_jwt = FALSE, bevidst — samme
  # begrundelse som ansoegning-gem og opret-indgangs-checkout: ingen session.
  # Legitimationen er tokenet i body (verifyAnsoegningslink, service-role-opslag
  # på ansoegninger.token hvor indsendt_at er sat — ingen SQL-funktion).
  [functions.ansoegning-link]
    verify_jwt = false
  # Ansøgerens booking/flyt/aflys af afklaringssamtalen (udkast 18/9): tiden
  # vælges her og oprettes i Calendly bagved. verify_jwt = FALSE af samme grund
  # som ansoegning-link — tokenet i
  # body er legitimationen (verifyAnsoegningslink FØR enhver service-role-handling).
  [functions.ansoegning-samtale]
    verify_jwt = false
  # Bucket B, samme form som indgangs-paamindelser-cron: authenticateServiceRole,
  # pg_cron via kald_edge (20260918210000) passerer gaten. Tørkørsel som standard.
  [functions.ansoegning-rykker-cron]
    verify_jwt = true
  # Metas annoncer (19/9-2026, recon-meta-annoncer). BEGGE er Bucket B, samme
  # form som indgangs-paamindelser-cron: authenticateServiceRole FØRST, kaldt
  # gennem kald_edge med vault-nøglen. meta-annonce-opslag skriver INTET (trin
  # 1: beviset for at utm_content er Metas ad_id); meta-annoncer-cron henter
  # sidste syv dage og har tørkørsel som standard. Ingen af dem skriver hos
  # Meta — kun ads_read.
  [functions.meta-annonce-opslag]
    verify_jwt = true
  [functions.meta-annoncer-cron]
    verify_jwt = true
  # Prøve-udløb for fremmøde (20/9). Bucket B, samme form som de øvrige croner:
  # authenticateServiceRole FØRST, kaldt gennem kald_edge. Bygger en PROEVE-
  # registrant, signerer den med webhookens egen nøgle INDEFRA og kalder den
  # rigtige ewebinar-webhook. Signerer ALDRIG en krop, kalderen har skrevet —
  # webinarId er låst til PROEVE-webinar, id'et skal starte med PROEVE-.
  [functions.ewebinar-proeve]
    verify_jwt = true
  # Klaviyos hændelser tilbage til os (19/9-2026, lag 5). Bucket B, samme form
  # som indgangs-paamindelser-cron: authenticateServiceRole FØRST, kaldt gennem
  # kald_edge med vault-nøglen. Tørkørsel som standard — uden «dry_run: false»
  # hentes der, men skrives ikke. Læser KUN hos Klaviyo (GET /events), skriver
  # intet derovre.
  [functions.klaviyo-hentning-cron]
    verify_jwt = true
  # De to klokker «betalt, ingen bruger» og «ingen login» (20/9-2026). Bucket B,
  # authenticateServiceRole FØRST, kaldt gennem kald_edge én gang i døgnet.
  # Tørkørsel som standard — uden «dry_run: false» dømmes der, men intet skrives.
  [functions.stille-klokker-cron]
    verify_jwt = true
  # Gensenderen for klaviyo_haendelser (21/9-2026). Bucket B, authenticateServiceRole
  # FØRST, kaldt gennem kald_edge hvert 5. minut. Tørkørsel som standard — uden
  # «dry_run: false» dømmes der, men intet sendes og intet mailes. Sender KUN
  # gemte kroppe igen gennem klaviyoAfsendelse (nøglen læses ét sted).
  [functions.klaviyo-gensend-cron]
    verify_jwt = true
  # Webinarets tidspunkt på Klaviyo-profilen (21/9-2026). Bucket B,
  # authenticateServiceRole FØRST, kaldt gennem kald_edge hver time. Tørkørsel som
  # standard — uden «dry_run: false» regnes der, men intet skrives. Skriver KUN de
  # to tb_naeste_webinar-felter gennem klaviyoAfsendelse (nøglen læses ét sted).
  [functions.klaviyo-profil-cron]
    verify_jwt = true
  # Metas Conversions API fra platformen (udkast 21/9-2026 aften). Bucket B,
  # authenticateServiceRole FØRST, kaldt gennem kald_edge hvert 5. minut. Tørkørsel
  # som standard; sender for alvor kun med «dry_run: false» OG (låsen
  # app_config.meta_send_aktiv ELLER en test_event_code). Læser KUN
  # META_SEND_TOKEN (metaSendAfsendelse.ts) — aldrig META_CAPI_TOKEN/META_ADS_TOKEN.
  [functions.meta-send-cron]
    verify_jwt = true
