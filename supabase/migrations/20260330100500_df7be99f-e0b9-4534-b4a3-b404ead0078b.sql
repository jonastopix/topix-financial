
-- The previous migration partially applied. Only the last policy failed (already exists).
-- Re-apply just that awareness — the other 4 policies were created successfully.
-- Nothing to do here, just a no-op marker.
SELECT 1;
